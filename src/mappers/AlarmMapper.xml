<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.vms.mapper.AlarmMapper">
    
    <select id="getAlarmList" resultType="com.vms.model.AlarmDto">
        SELECT 
            alarm_id AS alarmId,
            vessel_id AS vesselId,
            alarm_type AS alarmType,
            message,
            DATE_FORMAT(created_at, '%Y-%m-%d %H:%i:%s') AS createdAt
        FROM TB_ALARM
        WHERE 1=1
        <if test="startDate != null and startDate != ''">
            AND created_at >= CONCAT(#{startDate}, ' 00:00:00')
        </if>
        <if test="endDate != null and endDate != ''">
            AND created_at &lt;= CONCAT(#{endDate}, ' 23:59:59')
        </if>
        ORDER BY created_at DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="getAlarmCount" resultType="int">
        SELECT COUNT(*) FROM TB_ALARM
        WHERE 1=1
        <if test="startDate != null and startDate != ''">
            AND created_at >= CONCAT(#{startDate}, ' 00:00:00')
        </if>
        <if test="endDate != null and endDate != ''">
            AND created_at &lt;= CONCAT(#{endDate}, ' 23:59:59')
        </if>
    </select>
</mapper>